## 1. 渲染基础设施

- [x] 1.1 引入 Markdown 解析与安全清洗依赖，并在 Vue 2 + TypeScript 环境中完成接入
- [x] 1.2 提取共享的简历 Markdown 渲染逻辑，输出可复用的安全 HTML 内容

## 2. 页面与导出集成

- [x] 2.1 更新 `src/views/resume-version/editor.vue`，增加编辑模式 / 预览模式切换，并移除独立弹窗式预览交互
- [x] 2.2 更新 `src/views/resume-version/view.vue`，将简历详情页预览切换为真实 Markdown 渲染
- [x] 2.3 更新 PDF 导出容器，使其复用同一份 Markdown 渲染结果而不是纯文本内容
- [x] 2.4 为编辑页预览、页面预览和 PDF 导出补充适合简历场景的 Markdown 样式，并包含表格样式

## 3. 验证与收尾

- [x] 3.1 验证标题、列表、强调、引用、代码块和表格等 Markdown 语法在编辑页预览和详情页中显示正确
- [x] 3.2 验证编辑模式 / 预览模式切换交互符合预期，且不会丢失已编辑内容
- [x] 3.3 验证原生 HTML 标签不会作为格式化能力生效，渲染结果仅遵循受支持的 Markdown 语法
- [x] 3.4 验证 Markdown 简历导出的 PDF 结构与页面预览一致，并检查长内容分页表现
